def style(text: str, color: str = "") -> str:
    """Dummy style function that returns plain text."""
    return text

def sprint(text: str, color: str = "") -> None:
    """Dummy sprint function that just prints."""
    print(text)
    